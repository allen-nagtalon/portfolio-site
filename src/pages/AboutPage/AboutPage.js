import { Container } from '@mui/material'

const AboutPage = _ => {
  return (
    <Container
      sx={{
        width: '100vw',
        height: '100vh',
        color: 'blue'
      }}
    >
      This is the Landing page.
    </Container>
  )
}

export default AboutPage
